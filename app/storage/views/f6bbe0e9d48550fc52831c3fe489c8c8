<?php foreach($comments as $index => $comment): ?>
	<?php if($index != 0): ?>
		<hr>
	<?php endif; ?>

	<div class="row">
		<div class="col-xs-12">
			<div style="padding-left: 15px">
				<a class="pull-right btn btn-default btn-xs" href="<?php echo URL::route('dev.comments.show', $comment->id); ?>">View</a>
				<p >
					<?php if(!$comment->readed): ?>
						<i class="fa fa-circle" style="margin-left: -20px; margin-right: 6px; color:#158cba"></i>
					<?php endif; ?>
					By:
					<b><?php echo $comment->email; ?></b>
				</p>
				<p>
					On:
					<b><?php echo $comment->created_at; ?></b>
				</p>
			</div>
		</div>
	</div>
<?php endforeach; ?>

<?php echo $comments->links(); ?>