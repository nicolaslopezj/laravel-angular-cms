<?php foreach($entity->attributes as $index => $attribute): ?>
	<?php if($index != 0): ?> 
		<hr>
	<?php endif; ?>
	<h4>
		<b><?php echo $attribute->name; ?></b>
	</h4>
	<p>
		<b>Type:</b>
		<code><?php echo $attribute->type; ?></code>
	</p>
	<?php if($attribute->validations): ?> 
		<p>
			<b>Validations:</b>
			<code><?php echo $attribute->validations; ?></code>
		</p>
	<?php endif; ?>
	<p>
		<a class="btn btn-danger btn-xs" href="<?php echo URL::route('dev.entities.attributes.delete', [$entity->id, $attribute->id]); ?>">
			Delete
		</a>
	</p>
<?php endforeach; ?>

<hr>
<a class="btn btn-default" href="<?php echo URL::route('dev.entities.index'); ?>">Back</a>
<a href="<?php echo URL::route('dev.entities.database', $entity->id); ?>" class="btn btn-warning">Database</a>
<a href="<?php echo URL::route('dev.entities.delete', $entity->id); ?>" class="btn btn-danger">Delete</a>
<a href="<?php echo URL::route('dev.entities.attributes.create', $entity->id); ?>" class="btn btn-primary">Add Attribute</a>